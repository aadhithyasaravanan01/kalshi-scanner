# Copy to .env for LOCAL runs (the real .env is gitignored, never committed).
# On GitHub Actions this is provided by the repo secret DISCORD_WEBHOOK_URL instead.
DISCORD_WEBHOOK_URL=

# Optional: set to 1 to print alerts instead of posting to Discord (testing).
# DRY_RUN=1
